compile_glsl(dynamicuniform_glsl_shaders shaders/)
add_executable(dynamicuniform main.cpp)
target_link_libraries(dynamicuniform PRIVATE slim)
add_dependencies(dynamicuniform dynamicuniform_glsl_shaders)
